<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <gradient
            android:startColor="@color/colorLibraryButton"
            android:centerColor="@color/colorFavoritesButton"
            android:endColor="@color/colorPlaylistButton"
            android:angle="270"/>
    <corners
            android:radius="0dp"
            android:bottomLeftRadius="7dp"
            android:bottomRightRadius="7dp"/>
</shape>